fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
# Pinned explicitly: anyio only depends on exceptiongroup when python<3.11
# (marker-gated), but the FC runtime is python3.10. Any build done under a
# newer interpreter (3.11/3.12) would silently skip it and break cold start.
exceptiongroup>=1.0.2
# FC python3.10 runtime invokes HTTP handlers as WSGI; index.py adapts the
# ASGI FastAPI app via a2wsgi.
a2wsgi>=1.7.0
# PostgreSQL backend (PolarDB Serverless) — optional at runtime; the
# SQLite backend remains for local dev when DATABASE_URL is unset.
psycopg2-binary>=2.9.9
